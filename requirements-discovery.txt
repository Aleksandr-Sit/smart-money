# Тяжёлые зависимости ТОЛЬКО для еженедельного discovery (Контур 1).
# Отдельно от монитора: он крутится 24/7 и должен оставаться лёгким.
-r requirements-monitor.txt
dune-client>=1.7
duckdb>=1.0
pandas>=2.0
tenacity>=8.0
